version: "3.9"

services:
  app1:
    build: ./app
    environment:
      - INSTANCE_NAME=App-1

  nginx:
    image: nginx:latest
    ports:
      - "8080:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - app1